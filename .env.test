# Test Environment Configuration
NODE_ENV=development

# Database (adjust to your test database if needed)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/quoteprogen

# Session Secret
SESSION_SECRET=test-secret-key-for-testing-only

# Email Service (set to mock/test mode)
# Leave these empty to use Ethereal Email for testing
RESEND_API_KEY=
SMTP_HOST=
SMTP_USER=
SMTP_PASS=

# Server Port
PORT=5000
